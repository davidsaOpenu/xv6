proc assert_on_exit_status {{code 0} {wait_for_last_exit 1}} {
    # puts {[BYPASSED] assert_on_exit_status}

    expect {
        "$ "
    }

    send "echo $?\n"
    sleep 0.3
}

proc pid_check { expect_str } {
    send "pid\n"
    expect {
        expect_str
    }
}